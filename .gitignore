# Hotel ID database - the raw Revelex export (~85MB) for the deeplink tool.
# Kept in this folder for build-hotel-index.py but must NEVER be committed
# (it would bloat the styles repo).
GTCHotels.csv

# Generated by build-hotel-index.py: ~13k shard files, ~134MB on disk.
# Rebuild it with `python3 build-hotel-index.py`, never commit it.
deploy/

deploy.zip

# Local Netlify folder
.netlify
